apiVersion: v1
kind: Service
metadata:
  name: much-todo-backend-service
  namespace: starttech
spec:
  selector:
    app: much-todo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP